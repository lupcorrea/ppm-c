#ifndef PPM_DEFINITIONS_H_
#define PPM_DEFINITIONS_H_

/* Constant definition file. Please specify the meaning of each constant as you declare them. */

/* Constant samples. They have no description because we don't use them yet. */
const std::size_t kAlphabetSize = 10;
const std::size_t kMaxOrder = 3;
const std::size_t kMessageSize = 12;

#endif
